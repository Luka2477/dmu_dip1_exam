doctype html
html
  head
    title Express-app 3
    script(src='index.js' defer=true)
    style.
      .flex-hor {
        display: flex;
        align-items: center;
        margin-bottom: 5px;
      }
      p {
        margin: 0;
      }
      input {
        height: 20px
      }
  body
    h3 Opret person
    div.flex-hor
      p Navn:
      input#name(type="text")
    
    div.flex-hor
      p Adresse:
      input#address(type="text")
    
    button#create-user Tilføj person
    
    h3 Personer
    ul#users
      each user in users
        li #{user.name} lives at #{user.address}
      

